<?xml version = "1.0" encoding = "UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="User">

    <insert id = "register" parameterType = "com.travel.carRentals.database.model.User">
        INSERT INTO users (username, password, email) VALUES (#{username}, #{password}, #{email});
    </insert>

    <select id="findByUser" parameterType="java.lang.String" resultType="com.travel.carRentals.database.model.User">
         select * from users where username like #{username};
    </select>

    <select id="findByEmail" parameterType="java.lang.String" resultType="com.travel.carRentals.database.model.User">
        select * from users where email like #{email};
    </select>

    <select id="findByToken" parameterType="java.lang.String" resultType="com.travel.carRentals.database.model.User">
        select * from users where token like #{token};
    </select>

    <update id="updateUser" parameterType = "com.travel.carRentals.database.model.User">
            UPDATE USERS SET USERNAME = #{username},
            PASSWORD = #{password},
            EMAIL = #{email},
            TOKEN = #{token},
            tokenCreationDate = #{tokenCreationDate}
            WHERE ID = #{id};
    </update>

    <update id="updatePassword" parameterType = "com.travel.carRentals.database.model.User">
        UPDATE USERS SET USERNAME = #{username},
        PASSWORD = #{password},
        EMAIL = #{email},
        TOKEN = #{token},
        tokenCreationDate = #{tokenCreationDate}
        WHERE ID = #{id};
    </update>
</mapper>
